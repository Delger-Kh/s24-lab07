package frogger;

public class Records {
    public boolean addRecord(FroggerID id, PlayerProfile profile) {
        // Add logic to store record (e.g., in a list)
        return true; // Placeholder
    }
}